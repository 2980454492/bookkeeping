# 个人记账应用 — 需求文档

> 版本: **v1.0** | 日期: **2026-06-01** | 状态: **桌面端 V1 核心功能已实现**（Web / Android / 小程序未启动）

---

## 目录

0. [桌面端实现状态总览](#0-桌面端实现状态总览)
1. [项目概述](#1-项目概述)
2. [四平台技术方案](#2-四平台技术方案)
3. [功能需求](#3-功能需求)
4. [分类体系设计](#4-分类体系设计)
5. [数据存储设计](#5-数据存储设计)
6. [API 接口设计](#6-api-接口设计)
7. [非功能需求](#7-非功能需求)
8. [迭代计划](#8-迭代计划)
9. [确认记录](#9-确认记录)

---

## 0. 桌面端实现状态总览

> 以下对照 **当前仓库 `server/` 目录** 的实际交付情况（2026-06-01）。图例：✅ 已实现 · 🔶 部分实现 · ⏳ 未实现

| 模块 | 状态 | 说明 |
|------|:----:|------|
| 收支记录 CRUD | ✅ | REST API + 记账页列表/弹窗 |
| 分类管理 | ✅ | 一级/二级增删改、恢复默认、同步 `categories.json` |
| 筛选与排序 | ✅ | 日期区间、类型、分类、金额区间、**关键字**、时间/金额排序 |
| 列表加载 | ✅ | 首次 30 条，「加载更多」递增批次（+50、+70…），滚到底自动加载 |
| 记账页汇总卡片 | ✅ | 该月/上月/该年/去年收支（前端按日期聚合） |
| 统计页 | 🔶 | **年柱状图**（12 月）+ **月日历** + **日明细**；无独立 `/api/stats` |
| 饼图 / 折线图 | ⏳ | 需求保留，当前未做 |
| 导入导出 | ✅ | JSON / CSV / TSV / TXT / XLSX；导出支持筛选与重名策略 |
| 导入增强 | ✅ | 结构化错误（前后端展示）；**缺失分类自动新建** |
| 金额校验 | ✅ | 允许 **≥ 0**（含 0），禁止负数 |
| 预算管理 | ⏳ | P3，表结构已预留 |
| 删除分类前引用检查 | ⏳ | 当前可直接删除，历史记录仍保留原分类名快照 |
| 打包 `.exe` + WebView2 | ⏳ | 当前为 **浏览器访问** `http://127.0.0.1:18080` 或 `run.sh` |
| Web / 小程序 / Android | ⏳ | 见第 8 章迭代计划 |

**当前技术栈（与初版规划差异）**：

| 层级 | 规划（v0.4） | **当前实现（v1.0）** |
|------|-------------|---------------------|
| HTTP 服务 | C++ Drogon | **cpp-httplib**（单进程内嵌） |
| 前端 | Vue 3 + Naive UI + ECharts | **原生 HTML / CSS / JavaScript** |
| 数据库 | SQLite | SQLite（`data/bookkeeping.db`） |
| 运行形态 | WebView2 原生窗口 | **本地 HTTP + 浏览器**（Linux/WSL 已可日常使用） |
| Excel | libxlsxwriter / OpenXLSX | 自研 ZIP + XML 读写（与导出格式一致） |

---

## 1. 项目概述

### 1.1 项目目标

开发一款轻量级个人记账应用，覆盖 **PC桌面端（.exe）、Web网页版（URL）、微信小程序、Android APP（.apk）** 四大平台。

### 1.2 四平台交付形态一览

| 平台 | 交付物 | 数据存储位置 | 是否需要网络 | 数据隔离方式 |
|------|--------|:---:|:---:|------|
| **PC 桌面端** | `.exe` 安装包，用户安装后双击使用 | 本机 SQLite | ❌ 完全离线 | 一台电脑一份数据 |
| **Web 网页版** | 一个 URL 链接，浏览器打开即用 | 服务端 SQLite (或浏览器 IndexedDB) | ✅ 需要 | 一个浏览器一份数据（无登录态下用设备指纹/本地存储隔离） |
| **微信小程序** | 微信内搜索/扫码进入 | 服务端 SQLite，按微信 openid 隔离 | ✅ 需要 | 一个微信号一份数据 |
| **Android APP** | `.apk` 安装包，安装后打开即用 | 手机本地 Room(SQLite) | ❌ 完全离线 | 一台手机一份数据 |

### 1.3 核心原则

- **数据本地化优先**：PC 和 Android 完全离线，数据在用户自己设备上
- **无需登录**：无账号体系，打开即用，数据完全私有
- **一设备/一账号一套数据**：不做跨设备云端同步（V1）
- **分类可配置**：提供默认分类，用户可自由修改，分类数据不写死在代码中
- **后端 C++ 优先**：核心逻辑用 C++ 实现，如遇开发困难则降级为 Java/Python

### 1.4 用户画像

- 个人用户，日常记账
- 非财务专业，不需要复式记账
- 期望快速录入、快速查看统计

---

## 2. 四平台技术方案

### 2.1 架构总览 — 两条部署线

```
┌─────────────────────────────────────────────────────────────────┐
│                        离线线（无网络）                           │
│                                                                  │
│  ┌──────────────────────┐        ┌─────────────────────────┐    │
│  │   PC 桌面端 (.exe)    │        │    Android APP (.apk)    │    │
│  │                       │        │                          │    │
│  │  ┌─ 原生窗口 ───────┐│        │  Kotlin + Room           │    │
│  │  │ WebView 渲染      ││        │       ↓                  │    │
│  │  │ Vue 3 前端        ││        │  SQLite (手机)           │    │
│  │  └────────┬─────────┘│        │       ↓                  │    │
│  │           │ fetch      │        │  Jetpack Compose UI     │    │
│  │  ┌────────┴─────────┐│        └─────────────────────────┘    │
│  │  │ C++ Drogon (内嵌) ││        交付: .apk                    │
│  │  │ SQLite (本机)     ││        数据: 手机 Room               │
│  │  └──────────────────┘│                                       │
│  └──────────────────────┘                                       │
│  交付: .exe 安装包                                               │
│  数据: 本机 SQLite                                               │
│  形态: 原生桌面窗口（非浏览器）                                    │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                       在线线（需要网络）                          │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │              云服务器 (VPS)                                │   │
│  │  ┌──────────────────────┐    ┌──────────────────────┐    │   │
│  │  │  C++ Drogon API      │    │  Vue 3 静态文件       │    │   │
│  │  │  (业务逻辑 + SQLite)  │    │  (Web 前端)          │    │   │
│  │  └──────────┬───────────┘    └──────────┬───────────┘    │   │
│  │             └───────────┬───────────────┘                │   │
│  └─────────────────────────┼────────────────────────────────┘   │
│                             │                                    │
│              ┌──────────────┼──────────────┐                     │
│              │              │              │                     │
│        ┌─────┴─────┐  ┌────┴─────┐  ┌─────┴─────┐              │
│        │ Web 网页版 │  │微信小程序 │  │ (未来扩展) │              │
│        │ 浏览器访问  │  │ 扫码进入  │  │           │              │
│        └───────────┘  └──────────┘  └───────────┘              │
│        交付: URL 链接   交付: 小程序码                            │
│        数据: 服务端     数据: 服务端(按openid隔离)               │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

**关键结论**：
- **桌面端和 Android 离线**，PC 是原生桌面窗口（WebView 渲染 Vue 前端 + 内嵌 C++ 后端），Android 用 Kotlin + Room
- **Web 和小程序在线**，共用一个云服务器上的 C++ Drogon 后端
- **Vue 3 前端在 PC 和 Web 之间 100% 复用**（PC 用 WebView 加载，Web 通过浏览器加载）
- **C++ 代码在桌面端和云服务器之间 100% 复用**（同一套 Drogon 代码，两个部署目标）

---

### 2.2 各平台详述

#### 平台一：PC 桌面端 🖥️

> **V1 当前交付**：`bookkeeping` 可执行文件 + 浏览器访问 `http://127.0.0.1:18080`（见 `scripts/run.sh`）。  
> **后续目标**：内嵌 WebView2 原生窗口；前端可择机迁移至 Vue 3，后端可择机迁移至 Drogon（接口保持 REST 即可）。

> **核心思路（目标架构）**：C++ 主程序 + 内嵌 WebView 渲染前端。用户看到的是一个原生桌面窗口，不是浏览器。
> ```
> ┌─────────────────────────────────────┐
> │  bookkeeping.exe (原生窗口)          │
> │  ┌─────────────────────────────────┐│
> │  │  WebView (Edge WebView2 / CEF)  ││
> │  │  ┌───────────────────────────┐  ││
> │  │  │  Vue 3 前端 (Naive UI)    │  ││
> │  │  │  表格 / 表单 / 图表       │  ││
> │  │  └──────────┬────────────────┘  ││
> │  │             │ fetch localhost    ││
> │  └─────────────┼───────────────────┘│
> │                ▼                     │
> │  ┌─────────────────────────────────┐│
> │  │  C++ Drogon HTTP 服务 (内嵌)    ││
> │  │  监听 127.0.0.1:18080           ││
> │  │  → SQLite 读写                  ││
> │  └─────────────────────────────────┘│
> └─────────────────────────────────────┘
> ```
> **JS 做 UI ≠ 必须在浏览器打开**。WebView 是一种内嵌在原生程序里的"无边框浏览器引擎"，用户完全感知不到它的存在。

```
开发流程：
  Vue 3 源码 → Vite 构建 → 静态文件 (HTML/JS/CSS)
  C++ 源码   → CMake 编译 → bookkeeping.exe (内嵌静态文件 + WebView + Drogon)

用户使用：
  拿到 .exe 安装包 → 安装 → 双击桌面图标
  → 弹出原生桌面窗口（WebView 渲染记账界面，不是浏览器）
  → 窗口内 JS 自动 fetch localhost:18080 调用 C++ 后端
  → 数据存本机 SQLite → 完全离线
```

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| 窗口容器 | **C++ Win32 / GLFW** | 创建原生窗口 |
| WebView | **Edge WebView2**（Win10+ 自带）或 **CEF** | 在原生窗口中渲染 HTML/JS/CSS |
| 前端 | **Vue 3** + TypeScript + Vite | 与 Web 版 **100% 复用** |
| UI 框架 | **Naive UI** | 表格/表单/日期选择器/消息提示 |
| 图表 | **ECharts 5** | 柱状图/饼图/折线图 |
| 后端 | **C++ Drogon**（同进程内嵌） | 监听 `127.0.0.1:18080`，仅本机可访问 |
| 数据库 | SQLite (sqlite_orm) | `.db` 文件存于 `%APPDATA%/Bookkeeping/` |
| 打包 | NSIS / Inno Setup | 生成 `.exe` 安装包 |
| 网络 | **不需要** | 完整离线运行 |

**WebView 选型对比**：

| | Edge WebView2 | CEF (Chromium Embedded) |
|------|:---:|:---:|
| 集成方式 | Windows SDK，系统中已有或自动安装 | 随应用分发 ~100MB 的 Chromium 库 |
| 体积 | 小巧（WebView2 由系统提供） | 打包后体积较大 |
| 跨平台 | ❌ 仅 Windows | ✅ Windows / Linux / macOS |
| 开发复杂度 | 低，API 简洁 | 中，需编译 CEF 或使用预编译包 |
| 推荐场景 | V1 先做 Windows 版 | 后续如需 Linux/macOS 再迁移 |

> **推荐**：V1 使用 **Edge WebView2**（Windows 10/11 均已内置），打包体积小，开发简单。后续如需跨平台，可将 WebView 层替换为 CEF，前端和后端代码完全不用改。

**启动流程**：
```
bookkeeping.exe
  ├─ 1. 初始化 SQLite，检查/导入 categories.json
  ├─ 2. 启动 Drogon HTTP 服务 (127.0.0.1:18080)
  ├─ 3. 创建原生窗口，嵌入 WebView2
  ├─ 4. WebView 加载内嵌的 Vue 3 前端 → 界面显示
  ├─ 5. 前端 fetch localhost:18080 调用 C++ API
  └─ 6. 用户关闭窗口 → 进程优雅退出
```

---

#### 平台二：Web 网页版 🌐

```
用户收到一个 URL → 浏览器打开 → 看到记账界面 → 直接使用
```

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| 部署 | 云服务器 (VPS) + 域名 | 最低 1核1G 即可 |
| 后端 | **C++ Drogon** (部署在服务器) | 与 PC 端 **同一套 C++ 代码** |
| 数据库 | SQLite (服务端) | 每个浏览器通过设备指纹/Session 隔离数据目录 |
| 前端 | **Vue 3** (与 PC 端 **同一套前端代码**) | 部署为静态文件，由 Drogon 或 Nginx 托管 |
| 图表 | **ECharts 5** | 同 PC |
| 网络 | **需要** | 服务端需持续运行 |

**数据隔离方案（无登录）**：
```
用户首次访问 → 服务端生成唯一 token 存入浏览器 Cookie
             → 服务端为此 token 创建独立 SQLite 文件: data/<token>.db
             → 后续请求自动带 Cookie，路由到对应数据库
             → 清除浏览器 Cookie 则数据不可找回（可提供导出备份功能引导）
```

> ⚠️ **取舍说明**：无登录的 Web 版无法做到像 PC 端那样"完全不同用户之间隔离"。上述 Cookie + token 方案是轻量折中。如果未来需要更可靠的数据隔离，可以加一个简单的"设置密码"功能。

---

#### 平台三：微信小程序 📱

```
用户微信扫码/搜索 → 进入小程序 → 授权获取 openid → 自动绑定数据
```

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| 前端 | **微信原生框架** (WXML/WXSS/JS) | 或 TypeScript |
| UI 组件 | Vant Weapp | 微信生态成熟组件库 |
| 图表 | **ECharts 微信小程序版** | 与 Web 版配置可部分复用 |
| 后端 | **C++ Drogon** (与 Web 共用同一服务器) | 复用同一套 API |
| 数据隔离 | 微信 `openid` | 每个微信号对应独立 SQLite 文件 `data/<openid>.db` |

**数据隔离流程**：
```
用户进入小程序 → wx.login() 获取 code
              → 后端用 code 换取 openid
              → 后续请求携带 openid (后端签发 token)
              → 每个 openid 对应独立 SQLite
              → 换个微信号 = 全新数据
```

> ⚠️ 小程序需要：已备案域名 + SSL 证书 + 微信小程序审核。**与 Web 版共用服务器**，仅需额外开发小程序前端。

---

#### 平台四：Android APP 📲

```
开发完成 → 打包 .apk → 用户下载安装 → 打开即用
```

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| 开发工具 | **Android Studio** | 官方 IDE |
| 语言 | **Kotlin** | Google 推荐 |
| UI | **Jetpack Compose** | 声明式 UI |
| 数据库 | **Room** (SQLite) | 数据存于手机本地 |
| 图表 | **Vico** / MPAndroidChart | 柱状图/饼图/折线图 |
| 网络 | **不需要** | 完全离线运行 |
| 交付 | `.apk` 安装包 | 可直接发送或放网盘下载 |

> Android APP **完全独立于 C++ 后端**。业务逻辑用 Kotlin 重写（或通过 JNI 调用 C++ 核心库），数据存在手机 Room 数据库中。

---

### 2.3 代码复用全景图

```
                    ┌──────────────────────────────┐
                    │       C++ 业务逻辑库          │
                    │   CRUD / 统计 / 导入导出       │
                    │   (编译为静态库 .a / .lib)    │
                    └──────────────┬───────────────┘
                                   │
              ┌────────────────────┼────────────────────┐
              │                    │                    │
     ┌────────┴────────┐  ┌───────┴────────┐  ┌───────┴────────┐
     │  桌面端 httplib    │  │ 云端 Drogon     │  │ Android JNI   │
     │  (链接 C++ 库)   │  │ (链接 C++ 库)   │  │ (可选，调用库)  │
     └────────┬────────┘  └───────┬────────┘  └───────┬────────┘
              │                   │                   │
              │           ┌───────┴────────┐          │
              │           │ 同一套 API      │          │
              │           │ (记录/分类/统计) │          │
              │           └───────┬────────┘          │
              │                   │                   │
    ┌─────────┴──────────┐  ┌────┴─────┐    ┌───────┴────────┐
    │  Vue 3 前端 (PC)   │  │ Web 前端  │    │ Android UI     │
    │  加载本地静态文件   │  │ 同 PC     │    │ Compose (独立)  │
    └────────────────────┘  └──────────┘    └────────────────┘
                                   │
                            ┌──────┴──────┐
                            │ 微信小程序前端│
                            │ (独立开发)   │
                            └─────────────┘
```

| 制品 | 复用范围 | 复用率 |
|------|---------|:------:|
| C++ 业务逻辑库 | PC + 云端 Drogon + (Android JNI 可选) | 高 |
| Vue 3 前端 | PC 端 + Web 端 | **100%** |
| C++ Drogon API 层 | PC 端 + 云端 | **100%** |
| 微信小程序前端 | 独立开发 | 0% (但 API 复用) |
| Android UI | 独立开发 | 0% (但 C++ 库可 JNI 复用) |

### 2.5 推荐开发顺序

| 优先级 | 平台 | 理由 |
|:------:|------|------|
| **1** | PC 桌面端 (.exe) | C++ 偏好 + 前后端不分开开发 + 离线即可调试 |
| **2** | Web 网页版 (URL) | 与 PC 100% 复用前端 + 后端，部署到云服务器即可 |
| **3** | Android APP (.apk) | Kotlin 独立开发，Room 本地数据库 |
| **4** | 微信小程序 | 需云服务器 + 备案 + 审核，复用 Web 版后端 API |

---

## 3. 功能需求

### 3.1 记账记录 CRUD

每条记录包含以下字段：

| 字段 | 类型 | 必填 | 说明 |
|------|------|:----:|------|
| id | Integer(PK) | — | 自增主键 |
| datetime | DateTime | ✅ | 交易时间，**精确到分钟** `YYYY-MM-DD HH:MM` |
| type | Enum | ✅ | `income`(收入) / `expense`(支出) |
| amount | Decimal(10,2) | ✅ | 金额，**≥ 0**（允许 0，禁止负数） |
| category_l1 | String | — | 一级分类（可为空字符串） |
| category_l2 | String | — | 二级分类（仅支出常用；收入通常为空） |
| note | String | 否 | 备注 |

**操作要求**：

| 要求 | 状态 | 实现说明 |
|------|:----:|----------|
| 新增后即时刷新 | ✅ | `POST /api/records` → `loadRecords()` |
| 编辑保存 | ✅ | `PUT /api/records/:id` |
| 删除确认 | ✅ | `confirm()` 后 `DELETE` |
| 列表默认时间倒序 | ✅ | `sort_by=datetime&sort_order=desc` |
| 列表分页 / 加载更多 | ✅ | 首次 **30** 条；每次加载更多递增 **+50、+70、+90…**；支持滚到底自动加载；计数显示「已显示 N / 总计 M」 |

### 3.2 分类体系

> **详见第 4 章**

- 仅支持两级分类（一级 → 二级），仅**支出**有二级分类
- **收入**仅有一级分类，无二级分类
- 系统提供默认分类，存储在 `categories.json` 文件中
- 用户可对默认分类进行**增、删、改**
- 删除分类前检查是否有记录引用，有则提示用户 — **⏳ 未实现**（记录表存分类名快照，删除分类不影响历史记录）

### 3.3 筛选与排序

| 功能 | 优先级 | 状态 | 说明 |
|------|:------:|:----:|------|
| 日期筛选 | P0 | ✅ | 快捷（本月/上月/今年/全部）+ 自定义起止日期 |
| 分类筛选 | P0 | ✅ | 一级、二级联动下拉 |
| 类型筛选 | P0 | ✅ | 全部 / 收入 / 支出 |
| 金额范围 | P1 | ✅ | `amount_min` / `amount_max` |
| 排序 | P0 | ✅ | 按时间或金额，升序/降序 |
| 关键字搜索 | P2 | ✅ | 匹配备注、分类名、金额字符串 |

### 3.4 统计图表

#### 已实现（统计页 · 前端聚合）

| 能力 | 状态 | 说明 |
|------|:----:|------|
| 年视图柱状图 | ✅ | 选定年份 1—12 月收/支对比（CSS 柱图，非 ECharts） |
| 月视图日历 | ✅ | 按日汇总，点击日期查看当日明细 |
| 日视图记录表 | ✅ | 展示选中日期的流水列表 |
| 记账页汇总卡片 | ✅ | 该月/上月/该年/去年支出与收入（不受列表筛选影响） |

> 实现方式：统计页通过 `GET /api/records` 拉取区间内记录，在 **浏览器端聚合**；尚未提供第 6 章所述的 `/api/stats/*` 专用接口。

#### 规划中（尚未实现）

| 图表 | 用途 | 优先级 | 状态 |
|------|------|:------:|:----:|
| **饼图** | 分类占比 | P1 | ⏳ |
| **折线图** | 收支趋势 | P1 | ⏳ |
| 按周 / 自定义跨年柱状 | 任意区间多粒度 | P0（原规划） | ⏳ |
| 统计摘要 API | 结余、最大单笔、TOP 分类 | P0（原规划） | ⏳ |

#### 统计维度（原规划 · 待后端 API）

用户可**自定义时间区间**，支持按天/周/月/年粒度 — **⏳ 待 `/api/stats/chart` 等接口落地后实现**。

> ~~日均支出~~ — 已删除，不需要

### 3.5 预算管理（P3 低优先级）— ⏳ 未实现

- 用户可为**支出一级分类**设置月度预算上限
- 当月支出接近预算时（如超过 80%），界面给出提醒
- 超过预算时标记红色告警
- 预算数据存储在 SQLite `budgets` 表中（**表结构已在需求中定义，代码未接入**）

### 3.6 导入导出

| 格式 | 导入 | 导出 | 实现说明 |
|------|:----:|:----:|----------|
| JSON | ✅ | ✅ | nlohmann/json |
| CSV | ✅ | ✅ | 表头：时间,类型,金额,一级分类,二级分类,备注 |
| TSV | ✅ | — | 制表符分隔，规则同 CSV |
| TXT | ✅ | ✅ | 本应用「个人记账导出」文本格式 |
| Excel (.xlsx) | ✅ | ✅ | 自研 ZIP + sheet1 + inlineStr；**仅读第一个工作表** |

**导出（✅）**

- 入口：设置页 → 导出数据
- `POST /api/records/export`：支持筛选条件、格式、文件名
- 重名处理：`cancel` / `replace` / `keep_both`
- 文件写入**应用根目录**

**导入（✅）**

- 入口：设置页 → 导入数据
- `POST /api/records/import`：body 含 `format`、`content`、`content_is_base64`（xlsx）
- **全量校验通过后一次性写入**（事务批量插入）
- **缺失的一级/二级分类自动创建**（支出才建二级），并回写 `categories.json`
- 失败时返回 `error`、`error_code`、`detail`、`hint`、`row`；前端弹窗内展示错误详情
- 表头须为：`时间, 类型, 金额, 一级分类, 二级分类, 备注`；类型为「收入」/「支出」

### 3.7 数据持久化

- SQLite 单文件数据库，存放于应用数据目录
- 启动自动加载，操作即时持久化
- 分类数据存于 SQLite，同时支持从 JSON 文件初始化/恢复默认
- 手动备份功能：导出完整数据库为 `.db` 备份文件

---

## 4. 分类体系设计

### 4.1 设计原则

- 分类数据 **不写死在代码中**，存储在 `categories.json` 和 SQLite 表中
- 首次启动时，从 `categories.json` 读取默认分类写入 SQLite
- 后续用户通过 UI 修改分类，直接操作 SQLite
- "恢复默认"按钮可从 JSON 重新导入

### 4.2 默认分类（已确认）

#### 支出分类（一级 + 二级）

| # | 一级分类 | 图标 | 二级分类 |
|---|---------|:---:|------|
| 1 | 购物 | 🛒 | 日用百货、服饰鞋包、数码产品、美妆护肤、家居用品、食品生鲜 |
| 2 | 餐饮 | 🍽️ | 早餐、午餐、晚餐、零食饮料、外卖小吃、朋友聚餐 |
| 3 | 出行 | 🚗 | 公交地铁、打车/网约车、加油充电、停车费、火车高铁、飞机出行 |
| 4 | 休闲 | 🎮 | 电影演出、运动健身、游戏娱乐、旅游度假、宠物相关、兴趣爱好 |
| 5 | 居家生活 | 🏠 | 房租/房贷、水电燃气、物业费、网费话费、维修装修、家政保洁、日常杂货 |
| 6 | 文化教育 | 📚 | 书籍文具、课程培训、考试报名、学习用品、打印复印 |
| 7 | 健康医疗 | 🏥 | 门诊挂号、药品费用、检查费用、住院治疗、口腔眼科、保健品 |
| 8 | 其他 | 📦 | 礼品红包、公益捐款、快递物流、金融服务、其他杂项 |

#### 收入分类（仅一级，无二级）

| # | 一级分类 | 图标 |
|---|---------|:---:|
| 1 | 工资收入 | 💰 |
| 2 | 投资收益 | 📈 |
| 3 | 礼金红包 | 🎁 |
| 4 | 出售闲置 | 💱 |
| 5 | 其他收入 | 📦 |

> ✅ **已确认**：收入无二级分类；以上二级分类草案已确认。

### 4.3 分类存储格式

#### JSON 文件 (`categories.json`) — 默认分类模板

```json
{
  "version": "1.0",
  "updated": "2026-05-29",
  "expense": [
    {
      "name": "购物",
      "icon": "🛒",
      "subcategories": [
        "日用百货", "服饰鞋包", "数码产品",
        "美妆护肤", "家居用品", "食品生鲜"
      ]
    },
    {
      "name": "餐饮",
      "icon": "🍽️",
      "subcategories": [
        "早餐", "午餐", "晚餐",
        "零食饮料", "外卖小吃", "朋友聚餐"
      ]
    },
    {
      "name": "出行",
      "icon": "🚗",
      "subcategories": [
        "公交地铁", "打车/网约车", "加油充电",
        "停车费", "火车高铁", "飞机出行"
      ]
    },
    {
      "name": "休闲",
      "icon": "🎮",
      "subcategories": [
        "电影演出", "运动健身", "游戏娱乐",
        "旅游度假", "宠物相关", "兴趣爱好"
      ]
    },
    {
      "name": "居家生活",
      "icon": "🏠",
      "subcategories": [
        "房租/房贷", "水电燃气", "物业费",
        "网费话费", "维修装修", "家政保洁", "日常杂货"
      ]
    },
    {
      "name": "文化教育",
      "icon": "📚",
      "subcategories": [
        "书籍文具", "课程培训", "考试报名",
        "学习用品", "打印复印"
      ]
    },
    {
      "name": "健康医疗",
      "icon": "🏥",
      "subcategories": [
        "门诊挂号", "药品费用", "检查费用",
        "住院治疗", "口腔眼科", "保健品"
      ]
    },
    {
      "name": "其他",
      "icon": "📦",
      "subcategories": [
        "礼品红包", "公益捐款", "快递物流",
        "金融服务", "其他杂项"
      ]
    }
  ],
  "income": [
    { "name": "工资收入", "icon": "💰" },
    { "name": "投资收益", "icon": "📈" },
    { "name": "礼金红包", "icon": "🎁" },
    { "name": "出售闲置", "icon": "💱" },
    { "name": "其他收入", "icon": "📦" }
  ]
}
```

#### SQLite 表结构

```sql
-- 一级分类
CREATE TABLE category_l1 (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    name        TEXT NOT NULL,
    type        TEXT NOT NULL CHECK(type IN ('income', 'expense')),
    icon        TEXT DEFAULT '',
    sort_order  INTEGER DEFAULT 0,
    created_at  TEXT DEFAULT (datetime('now'))
);

-- 二级分类（仅支出用）
CREATE TABLE category_l2 (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    l1_id       INTEGER NOT NULL REFERENCES category_l1(id) ON DELETE CASCADE,
    name        TEXT NOT NULL,
    sort_order  INTEGER DEFAULT 0,
    created_at  TEXT DEFAULT (datetime('now'))
);
```

#### 初始化流程

```
首次启动 → 检查 category_l1 是否为空
         → 空则从 categories.json 读取写入 SQLite
         → 用户修改 → 直接操作 SQLite
         → 恢复默认 → 清空表 + 重新从 JSON 导入
```

---

## 5. 数据存储设计

### 5.1 数据库表结构

```sql
-- 记账记录表
CREATE TABLE records (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    datetime        TEXT NOT NULL,              -- 'YYYY-MM-DD HH:MM' 精确到分钟
    type            TEXT NOT NULL CHECK(type IN ('income', 'expense')),
    amount          REAL NOT NULL,              -- 金额，≥ 0
    category_l1     TEXT NOT NULL,              -- 一级分类名
    category_l2     TEXT DEFAULT '',            -- 二级分类名（收入为空串）
    note            TEXT DEFAULT '',
    created_at      TEXT DEFAULT (datetime('now')),
    updated_at      TEXT DEFAULT (datetime('now'))
);

-- 索引
CREATE INDEX idx_records_datetime ON records(datetime);
CREATE INDEX idx_records_type ON records(type);
CREATE INDEX idx_records_cat1 ON records(category_l1);
CREATE INDEX idx_records_cat2 ON records(category_l2);
CREATE INDEX idx_records_datetime_type ON records(datetime, type);

-- 预算表（P3 低优先级）
CREATE TABLE budgets (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    year        INTEGER NOT NULL,
    month       INTEGER NOT NULL CHECK(month BETWEEN 1 AND 12),
    category_l1 TEXT NOT NULL,
    amount      REAL NOT NULL,
    UNIQUE(year, month, category_l1)
);
```

### 5.2 文件布局

```
应用数据目录/
├── bookkeeping.db          # SQLite 主数据库
├── categories.json         # 默认分类模板
├── config.json             # 用户配置
├── backups/                # 备份目录
└── exports/                # 导出目录
```

---

## 6. API 接口设计

> PC 端通过 **cpp-httplib** 提供 REST API，默认 `http://127.0.0.1:18080`；静态前端挂载于 `/`。Web/小程序规划共用同一套接口；Android 使用 Room，不调此 API。

### 6.0 已实现接口一览（PC V1）

| 方法 | 路径 | 状态 |
|------|------|:----:|
| GET | `/api/health` | ✅ |
| GET/POST/PUT/DELETE | `/api/records`、`/api/records/:id` | ✅ |
| POST | `/api/records/export` | ✅ |
| POST | `/api/records/import` | ✅ |
| GET/POST/PUT/DELETE | `/api/categories`、`/api/categories/l1`、`/api/categories/l2` | ✅ |
| POST | `/api/categories/reset` | ✅ |
| GET | `/api/stats/*` | ⏳ |
| POST | `/api/budgets` 等 | ⏳ |

### 6.1 记录相关

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/records` | 查询记录列表 |
| GET | `/api/records/:id` | 获取单条记录 |
| POST | `/api/records` | 新增记录 |
| PUT | `/api/records/:id` | 修改记录 |
| DELETE | `/api/records/:id` | 删除记录 |

**查询参数**：
```
?keyword=午餐                         # 关键字搜索（P2）
&type=expense                         # 收支类型
&cat_l1=餐饮                          # 一级分类
&cat_l2=午餐                          # 二级分类
&date_from=2026-05-01                 # 起始日期
&date_to=2026-05-31                   # 结束日期
&amount_min=10                        # 最小金额
&amount_max=1000                      # 最大金额
&sort_by=datetime|amount              # 排序字段
&sort_order=asc|desc                  # 排序方向
&page=1&page_size=50                  # 分页（列表「加载更多」由前端递增 page_size 请求）
```

### 6.2 分类相关

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/categories` | 获取全部分类 |
| GET | `/api/categories?type=expense` | 按类型获取 |
| POST | `/api/categories/l1` | 新增一级分类 |
| PUT | `/api/categories/l1/:id` | 修改一级分类 |
| DELETE | `/api/categories/l1/:id` | 删除一级分类 |
| POST | `/api/categories/l2` | 新增二级分类 |
| PUT | `/api/categories/l2/:id` | 修改二级分类 |
| DELETE | `/api/categories/l2/:id` | 删除二级分类 |
| POST | `/api/categories/reset` | 恢复默认分类 |

### 6.3 统计相关

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/stats/summary?date_from=X&date_to=Y` | 区间概览（收支结余+最大单笔+TOP分类） |
| GET | `/api/stats/chart?granularity=day&date_from=2026-05-01&date_to=2026-05-31` | 按天柱状图 |
| GET | `/api/stats/chart?granularity=week&date_from=2026-01-01&date_to=2026-12-31` | 按周柱状图 |
| GET | `/api/stats/chart?granularity=month&date_from=2023-01-01&date_to=2024-12-31` | 按月柱状图（支持跨年对比） |
| GET | `/api/stats/chart?granularity=year` | 按年柱状图 |
| GET | `/api/stats/pie?type=expense&date_from=X&date_to=Y` | 分类占比饼图 |
| GET | `/api/stats/trend?granularity=day&date_from=X&date_to=Y` | 收支趋势折线图 |

**关键改进**：统计接口全部支持 `date_from` + `date_to` 自定义区间，用户可以自由选择任意时间段（如 2023-01 ~ 2024-12）。

**返回格式示例** (granularity=month)：
```json
{
  "granularity": "month",
  "labels": ["2023-01", "2023-02", ..., "2024-12"],
  "income":  [5000, 5000, ...],
  "expense": [3200, 4100, ...]
}
```

### 6.4 导入导出

| 方法 | 路径 | 状态 | 说明 |
|------|------|:----:|------|
| POST | `/api/records/export` | ✅ | body: `filename`, `format`, 筛选字段, `conflict_strategy` |
| POST | `/api/records/import` | ✅ | body: `format`, `content`, `content_is_base64`；响应含 `count`, `categories_created_l1/l2` |

**导入成功响应示例**：

```json
{
  "ok": true,
  "count": 120,
  "categories_created_l1": 2,
  "categories_created_l2": 1
}
```

**导入失败响应示例**：

```json
{
  "error": "无法识别 Excel 表头",
  "error_code": "xlsx_header_mismatch",
  "detail": "期望表头：时间, 类型, 金额…；实际首行：…",
  "hint": "请使用本应用导出的 .xlsx"
}
```

### 6.5 预算相关（P3）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/budgets?year=2026&month=5` | 获取月度预算 |
| POST | `/api/budgets` | 设置/修改预算 |
| GET | `/api/budgets/check` | 检查预算使用情况（超支告警） |

---

## 7. 非功能需求

### 7.1 可用性
- 新增记录 ≤ 3 步（选类型 → 填金额 → 选分类 → 保存）
- 常用功能入口 ≤ 2 次点击
- 界面语言：简体中文
- 桌面端/Web 前端遵循响应式设计

### 7.2 性能
- 10,000 条记录内，列表查询 < 200ms
- 统计图表数据计算 < 500ms
- 应用冷启动 < 2 秒

### 7.3 数据隐私
- 全本地存储，不上传数据到服务器
- PC 端 C++ 后端仅监听 `127.0.0.1`（localhost），不对外暴露
- Android APP 使用 Room 本地数据库，完全离线
- 微信小程序为唯一需要外网的端（平台强制要求）

### 7.4 可扩展性
- 保留多账户接口（V2）
- 保留货币符号配置扩展点

---

## 8. 迭代计划

### 第一阶段：PC 桌面端（核心）— 进度更新 2026-06-01

| 版本 | 内容 | 状态 |
|------|------|:----:|
| **V1.0** | C++ httplib 后端 + SQLite + 记录/分类 CRUD API | ✅ |
| **V1.1** | 前端：三页（记账 / 统计 / 设置）+ 列表增删改 + 筛选排序 + 关键字 | ✅ |
| **V1.2** | 分类管理 + categories.json 同步 + 恢复默认 | ✅ |
| **V1.3** | 统计页：年柱图 + 月日历 + 日明细（前端聚合） | 🔶 |
| **V1.4** | 饼图 + 折线图 + `/api/stats` 专用接口 | ⏳ |
| **V1.5** | 导入导出 JSON/CSV/TSV/TXT/XLSX + 导入错误细化 + 自动建分类 | ✅ |
| **V1.5.1** | 列表「加载更多」（递增批次 + 滚到底加载） | ✅ |
| **V1.6** | **PC 打包**：WebView2 原生窗口 + `.exe` 安装包 | ⏳ |
| **V1.7** | 删除分类引用检查；xlsx 多工作表兼容（可选） | ⏳ |

### 第二阶段：Web + 搜索 + 预算

| 版本 | 内容 | 交付物 |
|------|------|------|
| **V2.0** | 关键字搜索 | ✅（已提前在 PC V1 完成） |
| **V2.1** | 云服务器部署：Drogon + Vue 前端 + Nginx 反向代理 | 🎯 **Web URL** |
| **V2.2** | Web 端数据隔离（Cookie + token，无登录方案） | — |
| **V2.3** | 预算管理（月度预算设置 + 超支提醒） | — |

### 第三阶段：Android + 小程序

| 版本 | 内容 | 交付物 |
|------|------|------|
| **V3.0** | Android APP：Kotlin + Room + Compose 基础 CRUD | 🎯 **Android .apk** |
| **V3.1** | Android：分类管理 + 筛选排序 | — |
| **V3.2** | Android：统计图表（柱状/饼图/折线图） + 导入导出 | — |
| **V4.0** | 微信小程序：前端开发 + 与云端 API 对接 | 🎯 **小程序上线** |

### 第四阶段：打磨

| 版本 | 内容 |
|------|------|
| **V5.0** | UI 美化 + 深色模式 + 交互动效 |
| **V5.1** | 各平台功能对齐、Bug 修复 |

---

## 9. 确认记录

| # | 事项 | 决定 |
|---|------|------|
| 1 | PC 不用 Qt，前后端分离 | ✅ C++ Drogon + Vue 3，内嵌 .exe，本地 localhost 通信 |
| 2 | APP 仅 Android，Android Studio | ✅ Kotlin + Room + Jetpack Compose |
| 3 | 日期精确到分钟 | ✅ `YYYY-MM-DD HH:MM` |
| 4 | 关键字搜索 | ✅ PC V1 已实现（原规划 P2） |
| 16 | 金额允许为 0 | ✅ `amount >= 0` |
| 17 | 导入自动创建分类 | ✅ `ensureCategoriesForRecords` |
| 18 | 导入失败结构化提示 | ✅ `error_code` + 前端错误区 |
| 19 | 列表递增加载更多 | ✅ 30 + 50 + 70… |
| 5 | 统计自定义时间段 | ✅ 全部统计 API 加 date_from/date_to |
| 6 | 删"日均支出"卡片 | ✅ 已移除 |
| 7 | 二级分类草案确认 | ✅ 不变，8 个支出一级 + 6 个二级/类 |
| 8 | 收入仅 5 类，无二级 | ✅ 工资收入/投资收益/礼金红包/出售闲置/其他收入 |
| 9 | 加预算功能，低优先级 | ✅ P3，V2.3 实现 |
| 10 | 不要汇率/多币种 | ✅ 不纳入 |
| 11 | 图表：柱状+饼图+折线图 | ✅ 三者均支持 |
| 12 | **PC 交付 .exe，原生窗口，完全离线** | ✅ WebView2 内嵌渲染 Vue 前端 + C++ Drogon 内嵌，原生桌面窗口 |
| 13 | **Web 交付 URL 链接** | ✅ 云服务器部署，Cookie+token 数据隔离（无登录） |
| 14 | **微信小程序：一号一数据** | ✅ 按微信 openid 隔离 SQLite |
| 15 | **Android 交付 .apk，完全离线** | ✅ Room 本地数据库，不依赖服务器 |

| 20 | 实际技术栈 | ✅ httplib + 原生 JS（与 Drogon/Vue 规划可后续迁移） |

---

> **15 项产品决策仍然有效。** V1 核心已可日常使用（`scripts/run.sh` 或浏览器打开 `127.0.0.1:18080`）。  
> **Windows 安装包**：在 WSL 中运行 `./scripts/build-win.sh` 交叉编译生成 `.exe`，将 `dist/Bookkeeping/` 复制到 Windows 即可。  
> **建议下一步**：① 按 DESIGN 打出 Setup.exe 并虚拟机实测；② WebView2 壳替换 bat；③ `/api/stats` 与饼图/折线图。
